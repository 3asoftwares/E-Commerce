export default null;

## Configure (placeholder)

This is a minimal placeholder page used during local Storybook development.

Project structure highlights:

- `components/` — reusable UI components
- `stories/` — Storybook stories
- `utils/` — shared helpers

To start Storybook run `npm run storybook` from the package folder.
