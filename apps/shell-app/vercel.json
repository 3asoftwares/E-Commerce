{
  "buildCommand": "cd ../.. && yarn install && yarn workspace 3a-ecommerce-types build && yarn workspace 3a-ecommerce-utils build && yarn workspace 3a-ecommerce-ui-library build && yarn workspace @e-commerce/shell-app build",
  "outputDirectory": "dist",
  "installCommand": "yarn install",
  "framework": null,
  "devCommand": "yarn dev",
  "regions": ["iad1"],
  "env": {
    "NODE_ENV": "production"
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
