{
  "buildCommand": "cd ../.. && yarn install && yarn workspace @e-commerce/types build && yarn workspace @e-commerce/utils build && yarn workspace @e-commerce/ui-library build && yarn workspace shell-app build",
  "outputDirectory": "dist",
  "installCommand": "yarn install",
  "framework": null,
  "devCommand": "yarn dev",
  "regions": ["iad1"],
  "env": {
    "NODE_ENV": "production"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
