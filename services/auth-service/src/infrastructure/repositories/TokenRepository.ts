/**
 * Token Repository
 *
 * Database operations for Token entity:
 * - create: Create new token
 * - findByToken: Find token by value
 * - findByUserId: Find user's tokens
 * - delete: Delete token
 * - deleteExpired: Clean up expired tokens
 * - blacklist: Mark token as invalid
 *
 * Responsibilities:
 * - Manage refresh tokens
 * - Manage reset tokens
 * - Handle token expiration
 * - Token blacklisting
 */

// Token repository implementation
// export class TokenRepository { ... }
