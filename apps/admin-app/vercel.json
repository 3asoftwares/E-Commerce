{
  "buildCommand": "cd ../.. && yarn install && yarn workspace 3a-ecommerce-types build && yarn workspace 3a-ecommerce-utils build && yarn workspace 3a-ecommerce-ui-library build && yarn workspace @e-commerce/admin-app build",
  "outputDirectory": "dist",
  "installCommand": "yarn install",
  "framework": "vite",
  "devCommand": "yarn dev",
  "regions": ["iad1"],
  "env": {
    "VITE_ENV": "production"
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
